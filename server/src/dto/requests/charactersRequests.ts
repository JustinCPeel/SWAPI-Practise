export type CharacterComparisonRequest = {
  first: number;
  second: number;
};

export type CharacterFetch = {
  url: string;
};

export type ComparisonRequest = {
  primary: string;
  secondary: string;
};
